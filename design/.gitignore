# The seeded canvas is a generated file (~2.4 MB — it carries the whole design
# editor inside it), so it isn't stored here. The artboards next to this file
# are the real source; Claude rebuilds the canvas from them whenever the
# design changes.
sawubona-home-directions.html
